# 💻 Front-End & Back-End Architecture

### Front-End: Modern Web Development
**1. HTML5 Standard Structure:**
* **Semantic Elements:** การใช้ Tags สื่อความหมาย (`<article>`, `<section>`, `<nav>`) นอกเหนือจาก Doctype/Head/Body
* **SEO Fundamentals:** Meta tags และ Open Graph เพื่อการแสดงผลบน Search Engine

**2. CSS & Styling:**
* **Responsive Layouts:** Flexbox และ Grid system
* **CSS Pre-processors/Modules:** การจัดการ Scope ของ CSS ไม่ให้ทับซ้อนกัน

**3. TypeScript & React Logic:**
* **Static Typing:** การกำหนด Types (Interfaces/Generics) เพื่อความเสถียรของข้อมูล
* **React Internals:**
    * *Virtual DOM:* เข้าใจการ Render เพื่อประสิทธิภาพ
    * *Hooks Pattern:* Custom Hooks, `useEffect`, `useContext`
    * *State Management:* การจัดการ Global State ของแอปพลิเคชัน

### Back-End Integration (Concepts)
* **API Communication:** RESTful Methods (GET, POST, PUT, DELETE)
* **Async/Await:** การจัดการ Asynchronous Operations ระหว่างรอข้อมูลจาก Server